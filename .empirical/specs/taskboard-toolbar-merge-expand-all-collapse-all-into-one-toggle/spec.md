# Taskboard Toolbar Merge Expand All Collapse All Into One Toggle

## Request

> Taskboard toolbar: merge 'Expand all'/'Collapse all' into ONE toggle button (anything collapsed incl. mixed → offers 'Expand all'; all expanded → 'Collapse all'; label+icon reflect next action) AND add project quick-filter chips in the same toolbar row after the toggle — one small ghost-style chip per distinct item.project in the loaded issues (single-select toggle driving the same preferences.externalProjects filter the filter UI uses; aria-pressed active state; only render chips when >1 project exists). Surgical: reuse icon registry + component vocabulary, no theme/layout drift.

## Goal

Taskboard toolbar: merge 'Expand all'/'Collapse all' into ONE toggle button (anything collapsed incl. mixed → offers 'Expand all'; all expanded → 'Collapse all'; label+icon reflect next action) AND add project quick-filter chips in the same toolbar row after the toggle — one small ghost-style chip per distinct item.project in the loaded issues (single-select toggle driving the same preferences.externalProjects filter the filter UI uses; aria-pressed active state; only render chips when >1 project exists). Surgical: reuse icon registry + component vocabulary, no theme/layout drift.

## Acceptance Criteria

- [ ] [AC-1] Taskboard toolbar: merge 'Expand all'/'Collapse all' into ONE toggle button (anything collapsed incl. mixed → offers 'Expand all'; all expanded → 'Collapse all'; label+icon reflect next action) AND add project quick-filter chips in the same toolbar row after the toggle — one small ghost-style chip per distinct item.project in the loaded issues (single-select toggle driving the same preferences.externalProjects filter the filter UI uses; aria-pressed active state; only render chips when >1 project exists). Surgical: reuse icon registry + component vocabulary, no theme/layout drift.

## Scope

Small, localized, and reversible changes required by the request.

## Non-goals

Unrequested behavior or broader architectural changes.

## Verification

Verification is skipped in Fast. No test authoring, test execution, formal review,
or repository Context maintenance is required. Record the implementation outcome
honestly; promote this same spec to Complex for verified assurance.
