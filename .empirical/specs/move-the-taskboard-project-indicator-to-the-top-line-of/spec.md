# Move The Taskboard Project Indicator To The Top Line Of

## Request

> Move the taskboard project indicator to the top line of each issue row/card: top line becomes 'ISSUE-KEY' left with the ghost Cube+project chip pushed to the right (all-muted tone); title drops to its own line; chip removed from bottom/trailing meta. Applies to WorkItemRow and KanbanCard in plugins/taskboard/app.tsx. Approved variant from mockup grid: key-first, chip-right, muted.

## Goal

Move the taskboard project indicator to the top line of each issue row/card: top line becomes 'ISSUE-KEY' left with the ghost Cube+project chip pushed to the right (all-muted tone); title drops to its own line; chip removed from bottom/trailing meta. Applies to WorkItemRow and KanbanCard in plugins/taskboard/app.tsx. Approved variant from mockup grid: key-first, chip-right, muted.

## Acceptance Criteria

- [ ] [AC-1] Move the taskboard project indicator to the top line of each issue row/card: top line becomes 'ISSUE-KEY' left with the ghost Cube+project chip pushed to the right (all-muted tone); title drops to its own line; chip removed from bottom/trailing meta. Applies to WorkItemRow and KanbanCard in plugins/taskboard/app.tsx. Approved variant from mockup grid: key-first, chip-right, muted.

## Scope

Small, localized, and reversible changes required by the request.

## Non-goals

Unrequested behavior or broader architectural changes.

## Verification

Verification is skipped in Fast. No test authoring, test execution, formal review,
or repository Context maintenance is required. Record the implementation outcome
honestly; promote this same spec to Complex for verified assurance.
